{
  "name": "flaky-finder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "mkdir -p reports",
    "test": "NODE_OPTIONS=--enable-source-maps cucumber-js --parallel $(getconf _NPROCESSORS_ONLN)"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@types/node": "^24.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
